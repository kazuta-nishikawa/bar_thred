<div class="row py-5 px-4">
    <div class="col-md-9 mx-auto">
        <!-- Profile widget -->
        <div class="bg-white shadow rounded overflow-hidden">
            <div class="px-4 pt-0 pb-4 cover">
                <div class="media align-items-end profile-head">
                    <div class="profile mr-3">
                        <% if @user.profile_image.attached? %>
                            <%= image_tag @user.profile_image, fallback:"tender.png",size:"130x130", class:"rounded mb-2 img-thumbnail" %>
                        <% end %>
                        </div>
                    <div class="media-body mb-5 text-white">
                        <h4 class="mt-0 mb-0"><%= @user.username%></h4>
                        <p class="small mb-4"> <i class="fas fa-map-marker-alt mr-2"></i>New York</p>
                    </div>
                </div>
            </div>
            <div class="bg-light p-4 d-flex justify-content-end text-center">
                <ul class="list-inline mb-0">
                    
                    
                        <li class="list-inline-item">
                            <h5 class="font-weight-bold mb-0 d-block"><%= @count_recipes %></h5>
                            <small class="text-muted"> <i class="fas fa-image mr-1"></i>Recipes</small>
                        </li>
                        <li class="list-inline-item">
                            
                            <h5 class="font-weight-bold mb-0 d-block"><%= @count_followers %></h5>
                            <small class="text-muted"> <i class="fas fa-user mr-1"><%= link_to "Followers",followers_user_path %></i></small>
                        </li>
                        <li class="list-inline-item">
                            
                            <h5 class="font-weight-bold mb-0 d-block"><%= @count_followings %></h5>
                            <small class="text-muted"> <i class="fas fa-user mr-1"><%= link_to "Following",followings_user_path %></i></small>
                        </li>
                </ul>
            </div>
            <div class="bg-light p-4 d-flex justify-content-end text-center">
                    <% if user_signed_in? %>
                        
                        <% if @user.id == current_user.id %>
                                    <%= link_to "Edit profile",edit_user_path(@user),class:"btn btn-sm btn-outline-dark" %>
                                    <%= @user.email %>
                        <% end %>
                    <% end %>
                <!--フォローボタン-->
                <%= render 'relationships/follow_button', user: @user %>
            </div>
            <div class="px-4 py-3">
                <h5 class="mb-0">About</h5>
                <div class="p-4 rounded shadow-sm bg-light">
                    <p class="text break"><%= @user.profile%></p>
                </div>
            </div>
            <div class="py-4 px-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <h5 class="mb-0">Recipes</h5>
                </div>
                <div class="row">
                    <%= render 'recipes/recipe_list', recipes:@recipes %>
                </div>
            </div>
        </div>
    </div>
</div>